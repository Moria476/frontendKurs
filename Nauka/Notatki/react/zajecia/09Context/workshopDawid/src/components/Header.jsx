import React from "react";
import Toolbar from "./Toolbar";

// const Header = ({ text }) => {
const Header = () => {
  return (
    <div>
      <h1>Header</h1>
      {/* <Toolbar text={text}></Toolbar> */}
      <Toolbar></Toolbar>
    </div>
  );
};

export default Header;
